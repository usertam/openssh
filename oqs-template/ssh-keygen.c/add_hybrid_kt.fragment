{%- for sig in config['sigs'] %}
    {%- for variant in sig['variants'] %}
        {%- for classical_alg in variant['mix_with'] %}
		{ "{{ classical_alg.name }}_{{ variant['name']|lower|replace('_','') }}", "{{ classical_alg.name|upper }}_{{ variant['name'] }}", _PATH_HOST_{{ classical_alg.name|upper }}_{{ variant['name'] }}_KEY_FILE },
        {%- endfor -%}
    {%- endfor %}
{%- endfor %}

